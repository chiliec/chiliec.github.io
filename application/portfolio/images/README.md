# Portfolio images

All real screenshots, provided 2026-06-04:

| File | Content |
|---|---|
| `claudebar-1.png` | ClaudeBar popover in the macOS menu bar (usage windows + API credits) |
| `indonesian-1.png` | Bahasa Buddy — scenario menu with Telegram Stars subscription |
| `indonesian-2.png` | Bahasa Buddy — voice messages + comprehension quizzes |
| `symplast-1.png` | Symplast on iPad — AI-assisted consultation notes (demo data) |
| `symplast-2.png` | Symplast on iPad — patient billing and estimates (demo data) |

TalkNative (page 01) uses an inline SVG architecture diagram in `index.html` instead of
screenshots — the app is work-in-progress.

To swap an image: replace the file (keep the filename) and re-run `../../build.sh`.
